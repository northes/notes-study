```bash
vim -p xxx xxx
```

```
gt 下一个标签页
gT 上一个标签页
```

```
:tabnew 新建空白标签页
:tabedit 在新标签页打开文件
:tabs 列出所有标签页
:tabclose 关闭当前标签页
:tabonly 仅保留当前标签页打开
:tabn 切换到下一个标签页
:tabp 切换到上一个标签页
:tabfirst 切换到第一个标签页
:tablast 切换到最后一个标签页
:tabm 将当前标签页放置到指定位置，从 0 开始计数
```