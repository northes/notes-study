
## 调试

当前示例处于 `cache` 包下

```go
func init(){
	...
	go func() {  
		<-bingo.APP().StartedDone()  
	   const openID = "south"  
	   ctx := game.NewTestCtx(openID)  
	   player := GetOrCreateLoginPlayerByOpenID(ctx,openID)  
     
	   player.dosomething  
     
	   player.save(ctx)  
	}()

}
```