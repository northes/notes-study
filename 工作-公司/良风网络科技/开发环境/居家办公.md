## Windows

```shell
docker run -it --name golang -v C:/Users/North/go/src:/go/src -v C:/Users/North/go/pkg:/go/pkg -v C:/Users/North/.gitconfig:/etc/gitconfig -v C:/Users/North/.ssh:/root/.ssh -v C:/Users/North/go/bin:/go/bin -v /var/run/docker.sock:/var/run/docker.sock golang:latest
```

```shell
docker run --privileged -e DOCKER_TLS_CERTDIR="" -it --name docker -v C:/Users/North/go/src:/go/src -v C:/Users/North/go/pkg:/go/pkg -v C:/Users/North/.gitconfig:/etc/gitconfig -v C:/Users/North/.ssh:/root/.ssh -v C:/Users/North/go/bin:/go/bin -v E:/Workspace/Share:~/share docker:stable-dind
```

```shell
apt upgrade && apt update && apt install -y git-lfs vim
go env -w GOPRIVATE=gitlab.shouyouqianxian.com,git.northes.co
go env -w GONOPROXY=gitlab.shouyouqianxian.com
go env -w GONOSUMDB=gitlab.shouyouqianxian.com
go env -w GOPROXY=https://goproxy.cn,direct
chmod 600 ~/.ssh/id_rsa
```

安装Docker

```shell
apt-get install ca-certificates curl gnupg lsb-release

mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

apt-get update

apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```


初始化go

```shell
wget https://go.dev/dl/go1.19.3.linux-amd64.tar.gz
tar -zxvf go1.19.3.linux-amd64.tar.gz -C /usr/local

vim /etc/profile
export GO111MODULE=on 
export GOROOT=/usr/local/go 
export GOPATH=/go
export PATH=$PATH:$GOROOT/bin:$GOPATH/bin


```


Mac 报错 `permission denied`
- 返回上一级目录，执行
```shel
chmod -R 777 .
```
授权所有目录权限